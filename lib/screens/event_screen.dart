import 'package:flutter/material.dart';

class EventsScreen extends StatelessWidget {
  static const routName='event_screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Event Screen'),
    );
  }
}