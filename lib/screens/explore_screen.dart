import 'package:flutter/material.dart';

class ExploreScreen extends StatelessWidget {
  static const routName='explore_screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Explore_screen'),
    );
  }
}